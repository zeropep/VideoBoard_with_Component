<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.videoboard.boot.repository.MemberDAO">
    <insert id="insertMember" parameterType="MemberVO">
		insert into member (email, username, pwd, role) values (#{email}, #{username}, #{pwd}, "ROLE_USER")
	</insert>
	
	<select id="emailExist" parameterType="String" resultType="int">
		select count(email) from member where email = #{email}
	</select>
	
	<select id="nameExist" parameterType="String" resultType="int">
		select count(username) from member where username = #{username}
	</select>
	
	<select id="selectOneMember" parameterType="String" resultType="MemberVO">
		select mno, email, username, role from member where username = #{username}
	</select>
	
	<select id="selectOneLogin" parameterType="MemberVO" resultType="MemberVO">
		select email from member where email = #{email} and pwd = #{pwd}
	</select>
	
	<select id="selectAllMember" resultType="MemberVO">
		select * from member
	</select>
	
	<select id="selectOneByEmail" resultType="MemberVO"> 
		SELECT mno, email, username, pwd, role FROM member WHERE email = #{email} 
	</select>
</mapper>